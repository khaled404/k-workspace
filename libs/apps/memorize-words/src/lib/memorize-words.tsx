/* eslint-disable-next-line */
interface MemorizeWordsProps {}

function MemorizeWords(props: MemorizeWordsProps) {
  return (
    <div>
      <h1>Welcome to MemorizeWords!</h1>
    </div>
  );
}

export default MemorizeWords;
