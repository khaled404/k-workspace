{
  "root": "apps/eisenhower-app",
  "sourceRoot": "apps/eisenhower-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/eisenhower-app",
        "outputPath": "dist/apps/eisenhower-app"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "eisenhower-app:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "eisenhower-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "eisenhower-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/eisenhower-app"],
      "options": {
        "jestConfig": "apps/eisenhower-app/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/eisenhower-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
